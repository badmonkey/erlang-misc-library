{application, erlib_base,
[
    {description, "erlib_base"},
    {vsn, "1.0.0"},
    {modules,
        [ behaviour, supervisor_child, jsonx, jsonx_search, jsonx_replace
        , binencoder, bindecoder, property, typed_property
        , uuid, router, error, type, hof, getopt, text
        ] },
    {registered,
        [ router
        ] },
    {applications,
        [ kernel, stdlib, crypto
        , lager
        ] },
    {env, []}
]}.

